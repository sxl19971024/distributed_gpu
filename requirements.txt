# ============================================
# distributed_gpu 核心依赖
# 目标环境: OpenMPI 4.1.5 + CUDA 12.1
# ============================================
# 推荐安装方式 (自动读取 pyproject.toml):
#   pip install -e .
#
# 或使用本文件:
#   pip install -r requirements.txt
#
# 若需精确指定 CUDA 12.1 版本的 PyTorch:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ============================================

torch>=2.1.0
mpi4py>=3.1.0
numpy>=1.21.0
opt-einsum>=3.3.0
